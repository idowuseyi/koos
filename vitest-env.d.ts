/// <reference types="vitest/globals" />
// Registers Vitest globals and jest-dom matcher types for the TypeScript program
// so test files type-check during `next build` (which checks all **/*.ts/tsx).
import "@testing-library/jest-dom/vitest";
